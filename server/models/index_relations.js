const models = require('./');
const JobType = models.job_type;
const Invoice = models.invoice;

module.exports = function(models) {
    //Relationships
    // JobType.hasMany(Invoice);
    // invoice_status.hasMany(Invoice);
    // customer.hasMany(Invoice);
    

}